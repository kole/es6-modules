{
  "name": "modules",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run minify && npm run server",
    "server": "node --experimental-modules build/serve.mjs",
    "minify": "minify --sourceType module src/js -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connect": "^3.6.6",
    "serve-static": "^1.13.2"
  },
  "devDependencies": {
    "babel-minify": "^0.5.0"
  }
}
